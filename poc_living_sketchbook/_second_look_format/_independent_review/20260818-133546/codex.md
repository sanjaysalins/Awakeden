# Independent review — codex (OK, 224s)

Findings:

1. §0 says “`_SECOND_LOOK_RECOMMENDATION.md`” backs the plan, but that file is not present where cited. The approval/6-step plan is therefore not auditable from the artifact.

2. §0 says “nothing rendered under this spec yet,” but the workspace already contains `_plants_nbp.py`, `_plants/1_moses_raises.png`, and a `second_look_plants` spend ledger row. The plan is stale against the real repo.

3. §3 says the snap uses “the caption stage’s existing word-alignment JSON,” but the existing `look_and_live/_alignment.json` is for the older narrator script, not Christ speaking John 3:14-15 KJV. The “lifted” timestamps belong to different wording.

4. §4 says “Render only the 4 Act-1 stills,” but the actual script renders 5 images because Plant 2 has `2a` and `2b`. Small mismatch, but it proves the spec is not the executable plan.

5. §3-§4 assumes “panel review” is running in parallel, but the saved cursor/gemini reviews failed on workspace trust prompts. That is not a healthy external panel.

6. Plant 3 claims “low sun” is “already established in the Noon Frieze default palette.” The Noon Frieze script/renders establish bright sun-bleached daylight, not the long-shadow lighting this plant needs.

7. Plant 2 is called “highest-risk” and “highest-impact,” and it needs a second matched Christ asset, but there is no kill/fallback rule if the blind test or silhouette match fails.

8. §4’s blind-read test is too vague: “fresh reviewers,” “anyone naming,” and “nobody senses” define no sample size, threshold, reviewer source, or logging method. It cannot support a go/no-go decision.

9. The plan omits cost gates. Even “before any animation spend” still involves metered NBP stills, variants, second assets, audio, alignment, and likely animation. No budget or explicit spend checkpoint is stated.

10. §1 says the “final frame matches the opening frame exactly,” but the project’s locked assembly rules require the cut to close on Christ/gospel-pivot. The plan acknowledges the delayed-Jesus exception, but not this separate landing-frame exception.

VERDICT: REVISE
TOP FIXES:
1. Replace the invalid “existing word-alignment JSON” claim with a concrete audio-synthesis and alignment step for the new John 3:14-15 Jesus-voice turn.
2. Add hard go/no-go criteria for Plant 2 and the blind test, including sample size, pass thresholds, and fallback if the plant fails.
3. Reconcile the plan with the real repo state: correct missing/stale file references, fix the broken panel-review workflow, add cost gates, and state which existing assembly gates are reused or explicitly waived.
