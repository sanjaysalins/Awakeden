You are an INDEPENDENT, ADVERSARIAL reviewer. You did NOT write this and you
are NOT here to praise it or rewrite it. Find the problems. Default to skepticism.

ARTIFACT TYPE: plan

LENS — judge this PLAN on (be specific, cite the exact step/claim):
- Feasibility against the real codebase / tools (does it assume things that exist?).
- Hidden risks, false assumptions, single points of failure.
- Over-engineering / premature building before the idea is proven.
- Missing steps, edge cases, or verification gaps.
- Reuse: does it duplicate tools that already exist instead of reusing them?
- Cost / spend and whether it's justified.

Read the artifact below. Give concrete, specific findings (cite the exact line/phrase).
Then end your reply with EXACTLY this block (on the VERDICT line write exactly ONE word —
PASS or REVISE or FAIL — never the list of options):

VERDICT: <PASS or REVISE or FAIL>
TOP FIXES:
1. <most important fix>
2. <second>
3. <third>

----- ARTIFACT START -----
# CORPUS ROLLOUT PLAN v5 — gold-master format × 12 living-page shorts (2026-07-14)

**Status:** v5 after panel round 4 (FULL panel 5/5, all REVISE — every finding verified,
fixed in code or answered below; grok's round-4 consensus: "A(a) $0 authoring can proceed
after tooling/SOP fixes; paid renders should not" — A(b) stays blocked on a clean verdict).
**Baseline gate FAILs for all 12 pieces: `batches/_rollout/baseline_gate_fails_20260714.txt`**
(the checked-in artifact round 4 asked for).
**User approval:** GO at 485cr HARD envelope (pilot spend COUNTS INSIDE it — zero point
defined below). Wave A starts only on a clean panel verdict for this revision.
Out of scope: EW01–09, QJA, and `father_forgive_them` (Wave E, separately quoted).

## Machine bar (all fail-closed, tested — 21 gate tests green)

- `pipeline/rollout_gate.py`: ≤60% full-bleed · ≥3 templates · still ≤2 uses · full-bleed
  repeats only as ≤2 adjacent two-shots · fx ≥50% of beats · **temp grade REQUIRED**
  (rays-only fx fails; cool pole ≥7000K; landing = warmest ≤5500K) · `motion:"smooth"` ·
  `cut_ticks:false` · ≥2 living_light clips, each actually PLAYED (not dyncam'd) · landing
  lit · no double-lighting · no dash/`…`/mojibake captions.
- **Gate fires on EVERY `run_piece --stage animate` where a livingpage spec exists**
  (round-2 fix: the living_light-presence condition left a mid-migration bypass window).
  `JITB_SKIP_ROLLOUT_GATE=1` = loud, discouraged escape for surgical repairs only.
- **Stop-loss is fail-closed AT THE CHOKEPOINT (round-3 fix):** `run_piece --stage animate`
  calls `pipeline.rollout_spend.check()` after the rollout gate and refuses paid renders
  (exit 4) when the 485cr cap is reached — no longer a manual wave-gate script (though it
  still runs there for the human view). Counts attributable ledger rows (Kling clips ×
  7.5cr; BytePlus stills separate USD) **cross-checked against rendered mp4s on disk and
  charged at the HIGHER number** (round-3 claude flag: the ledger writer is best-effort —
  `hf_animate` proceeds unmetered if `pipeline.cost` errors — so ledger alone can
  under-count; the disk check catches that class). Remaining stated limits: (a) `cost
  summary` credits can't see animate rows; (b) account `reconcile` over-attributes under
  parallel use — excluded; (c) failed-but-BILLED rolls (502s, NSFW blocks) leave neither
  row nor mp4 — HF balance eyeball at each wave gate covers them, and the forecast carries
  a line for NSFW-blocked-but-billed rolls on crucifixion imagery. **ONE TRUTH for current
  spend (round-5 fix, all sections agree): 5 clips = 37.5cr, remaining 447.5cr.** The
  stop-loss refuses PROJECTED breaches (spent + one clip > cap), not just realized ones,
  and a bare `--stage animate` on a rollout piece refuses when >2 clips would render
  (BULK GUARD, exit 5; `JITB_ALLOW_BULK_ANIMATE=1` = loud override). The 7.5cr constant
  is pinned in the gate: `animate.duration != 5` is a gate FAIL until the meter is re-pinned.
- **One render path, no silent fallback:** ALL rollout renders and re-rolls go through
  `run_piece --stage animate [--only <slug>]` — never the `_hf_animate_short.py` CLI or
  `_animate_rerolls.py` (both bypass the rollout gate, and the CLI silently substitutes a
  $0 ffmpeg crop-cut on NSFW blocks, which would let a static fake occupy a living-light
  slot). Verified: `run_piece` prints FAILED and substitutes nothing — an HF block is a
  visible re-roll decision, never a silent downgrade.

## Human checklist per piece (recorded as `visual/wave_checklist.json`, reviewed at wave gate)

1. Scale variety (CU+wide+detail+medium) · 2. grids only on multi-figure stills, Christ
singles stay full-bleed · 3. audio-diff: spec beat `sfx` arrays vs the piece's REAL bed
builder — `sfx_pilots/build_cluster1_sfx.py` PIECES dict or the piece's `build_*_sfx.py`,
as discovered by `cli_livingpage._sfx_builder()` (round-3 fix: no per-piece `_sfx.py`
exists; the old grep target was wrong) · 4. hook-open / Christ-close bookend · 5. filmstrip
per new clip via the gold-master ffmpeg-strip workflow, shipped as a shared helper in the
A(a) tooling (round-3 fix: `pipeline/clip_anim_qc.py` is v1 scene-plan layout and no-ops on
cluster pieces — reference dropped); rejects parked in `visual/clips/_rejected/` ·
6. BEFORE/AFTER page + backup via the shared tooling · 7. builder fit-gate warnings
reviewed and dispositioned by eye (advisory by design — the gold master itself carries 2
accepted warnings, so it cannot hard-block).

## Waves

| Wave | Pieces | Gate |
|---|---|---|
| A(a) | it_is_finished, pierced, crucifixion_foretold — SPEC AUTHORING (a full gold-master rewrite ×3, NOT a light touch — panel is right that this is the biggest wall-clock block): baseline FAILs already logged (`_rollout/baseline_gate_fails_20260714.txt`) → grid conversion (SOP: Christ singles stay full; convert crowd/object/multi-figure beats; preserve each piece's heartbeat/punch/whip choreography) + fx arc + smooth + living_light entries NAMED per piece before A(a) ends → gate PASS. **Deliverable: shared tooling, re-aimed at the production path (round-4 fix):** thin cluster wrapper over the EXISTING `pipeline.clip_anim_qc.build_filmstrip()` (don't reinvent) + backup helper + before/after page builder + `wave_checklist.json` writer (schema: per-item pass/fail + note + reviewer + date). The gold-master promote script stays a pilot utility only — production renders go straight to `clips/` via `run_piece --only`. $0. | — |
| A(b) | Wave A renders: 2 living-light clips/piece (6 total), **first roll = the wound/CU proof** (`it_is_finished`'s bowed-head/nail still — names finalized at authoring, logged in wave_checklist) → filmstrip QC → measured re-roll rate | HUMAN + stop-loss |
| B | forsaken_cry, i_thirst, into_thy_hands, today_paradise | **RE-QUOTE FIRST** (below) |
| C | watch_one_hour, woman_behold, thirty_pieces | HUMAN + stop-loss |
| D | empty_tomb, sign_of_jonah — fact cards FIRST → author still jobs → `run_piece.reuse_check` → render only the gaps → stills-gate → **animate → register** (round-4 gemini: register scans `clips/` for mp4s, so it must FOLLOW animate — ordering corrected) | HUMAN + stop-loss |
| E | father_forgive_them — greenfield mocomic→livingpage migration; scoped + quoted separately after Wave A. **Wave E must never move/delete assets other pieces reference** (`it_is_finished` refs `../father_forgive_them/visual/_byteplus/face_on_cross_fix2.png`) | — |

Reuse discipline applies to ANY still work in ANY wave (not only D): fact cards → author
jobs → `reuse_check` → render only the gaps. A(b) is BLOCKED until the A(a) tooling exists.

Per piece: backup final (`.bak_prelivinglight`) → spec upgrade → gate PASS → `rollout_spend`
+ `cost summary --episode` pre-flight ($25/short ceiling; a near-ceiling episode = ASK THE
USER, never silent `override=True`) → renders → QC → rebuild → before/after page → register.
**The wave-gate user review of before/after pages IS the re-approval of shipped finals**
(answer to "run independent_review on every rebuild": the standing panel rule covers locked
narrations + significant plans — this plan — not every deterministic rebuild; the narrations
are unchanged).

## Budget v5 (re-based on the REMAINING envelope — round-4 claude catch)

- Zero point: rollout spend to date = **37.5cr** (ledger = disk after de-dup fix; billed
  possibly ~+15cr in 502s — limit (c), balance eyeball). **Remaining envelope ≈ 447.5cr.
  All forecasts below compare against the REMAINING number, not 485.**
  (gemini's "start date leaks July 12–13 pilot spend" is answered: that spend was the
  Women PIECE's own production — voice/stills/12 camera-only clips under its $6 episode
  budget, approved and reconciled separately. The 485 envelope covers ROLLOUT work, which
  began 2026-07-14. The envelope's zero point is a definition, and it is now stated.)
- Living-light: 12 pieces × 2 clips = 24 × 7.5cr = **180cr base**
- Wave D de-dup **re-derived from the actual baseline gate dump** (round-4 fix): most
  full-bleed doubles are NON-adjacent repeats fixable by $0 grid conversion; the only
  true still-overuse is empty_tomb's `risen_christ_wounds` ×5 → ≥3 beat reassignments,
  of which ~2–3 need NEW stills+clips. Wave D extra ≈ **15–30cr** + ~$0.15 BytePlus
  (was "67.5cr", an estimate dressed as derived — panel was right).
- Base ≈ 195–210cr. Worst case (pilot 2.3×/keeper): **~449–483cr vs 447.5 remaining —
  right at the wall**; hoped ~1.5×: **~295–315cr, ~135cr headroom.**
- **Forecast rule: B–D forecast at max(Wave A measured, 2.3×) unless Wave A shows ≤1.5×
  across all 6 rolls. Re-quote before Wave B either way. Per-slug roll cap: 3 attempts,
  then stop and report (pilot precedent) — one stubborn CU cannot eat a wave's budget.**
- **A(b) invocations MUST use `--only <ll_slugs>`** — a bare `--stage animate` re-renders
  every hash-stale clip and burns the envelope on non-living-light work (round-4 grok).
- **Default scope cuts if forecast > remaining** (user picks at re-quote): (1) 1 living-light
  clip (landing only) on B–C, saves ~52–120cr; (2) defer Wave D de-dup, saves ~15–30cr;
  (3) drop pieces. No silent spending through the cap.

## Re-roll mechanic (round-4 fix — now real in code)

QC reject → park the mp4 in `visual/clips/_rejected/` (leave the `.src.sha`) → the clip
state reads **missing** → `run_piece --stage animate --only <slug>` re-renders it. (The
skip-if-hash-current logic previously made a parked reject SKIP forever; fixed.) The
stop-loss re-checks the cap **before every paid clip**, not once per invocation, and all
gate/stop-loss refusals now propagate to the shell exit code (round-4 codex: `main()`
was swallowing them — automation saw success on refused runs).

## Answered panel objections (round 4)

- **codex "exit 4 never reaches the shell":** TRUE and fixed — `main()` now propagates
  stage return codes (was returning 0 on refused runs).
- **3 voices "stop-loss checks once, batch can overshoot":** fixed — re-check before
  EVERY paid clip in the loop.
- **claude/codex/grok "disk count double-counts promotes, misses parked rejects":** fixed —
  recursive glob incl. `_rejected/`/`_stale_from_bad_stills/`, de-duped by (size, mtime)
  copy2 signature; tested (`test_disk_clip_count_dedup_and_rejects`). Live reading now
  agrees with the ledger (5 = 5).
- **claude "forecast ignores its own zero point":** fixed — Budget v5 compares against the
  447.5cr REMAINING envelope.
- **gemini "ROLLOUT_START leaks pilot spend":** answered (not a bug) — July 12–13 spend was
  the Women piece's own production budget, not rollout work; zero point now defined in text.
- **gemini "no re-roll path / needs --force":** fixed differently — a parked reject reads
  as clip-state "missing" and re-renders; no new flag needed.
- **gemini "register before clips":** TRUE — Wave D ordering corrected (animate → register).
- **grok/cursor "A(a) tooling vapor / mis-aimed":** re-aimed — reuse `build_filmstrip`,
  production path is `run_piece --only`, promote demoted to pilot utility; tooling remains
  an A(a) deliverable that BLOCKS A(b).
- **grok/codex "Wave D numbers soft":** re-derived from the checked-in baseline dump
  (15–30cr, grid conversion absorbs the non-adjacent doubles).
- **cursor "spec-less pieces bypass gate+stop-loss":** answered — every run_piece-runnable
  piece has both `piece.json` and a livingpage spec today; `father_forgive_them` has
  neither (cannot run through run_piece at all) and is Wave E scope. Caveat now stated.
- **codex ".venv python would not launch in this environment":** reviewer-sandbox
  limitation, not a repo defect; suite verified locally (314 passed / 1 skipped).

## Answered panel objections (rounds 2–3)

- **round-3 claude #1 (unmetered-spend hole):** fixed — disk cross-check in `rollout_spend`,
  charged at max(ledger, disk); stated limit (c) covers billed-but-failed rolls.
- **round-3 claude #2/#4 + cursor (fallback/re-roll path, QC tooling, audio target, fit-gate,
  reuse in A–C, Wave E dependency):** all folded into the sections above.
- **round-3 cursor "stop-loss not at chokepoint":** fixed — `run_piece` animate now refuses
  on breach (exit 4).

- **gemini "BytePlus unsupported" (round 1):** wrong — `run_piece.py:34` ARK endpoint; all
  cluster stills render there. Confirmed by grok + claude round 2.
- **gemini "reuse_check before fact cards impossible":** correct — Wave D ordering fixed
  above (fact cards → jobs → reuse_check → render gaps).
- **grok "stop-loss is a false control":** correct — replaced with `pipeline/rollout_spend.py`
  (mechanics + limits above).
- **cursor "Wave A is heavy authoring, not a preflight":** correct — Wave A split into A(a)
  authoring/$0 and A(b) render/rate-measurement, with tooling as an explicit deliverable.
- **claude "gate window / zero point / derive Wave D":** all fixed above.
----- ARTIFACT END -----
